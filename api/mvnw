#!/usr/bin/env sh

set -e

if command -v mvn >/dev/null 2>&1; then
  mvn "$@"
  exit $?
fi

echo "Apache Maven is required to run this project. Install Maven or use a JDK that bundles it." >&2
exit 1
